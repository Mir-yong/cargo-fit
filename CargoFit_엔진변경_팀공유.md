# 해 선택 엔진 변경 — 팀 공유

작성일 2026-09-16 · 대상 파일 `clp/solutions.py`, `web/index.html`

---

## 한 줄 요약

**탐색 알고리즘(NSGA-II)은 한 줄도 바뀌지 않았다.**
바뀐 건 "이미 찾아낸 파레토 프론트에서 어느 해를 화면에 보여줄지 고르는 방식" 하나다.

---

## 0. 안 바뀐 것부터 — 기존 실험 수치 그대로 쓸 수 있음

| 항목 | 상태 |
|---|---|
| `clp/nsga2.py` | **무변경** (최종 수정 8/24). 탐색·교차·변이·다양성 주입 그대로 |
| 목적함수 Z1 / Z2 / Z3 | 그대로 |
| DBLF 디코더, 물리 제약(지지면적·적재단수·무게중심·중량한도) | 그대로 |
| 3종 프리셋 카드(최대 용적률 / 균형 / 최소 리핸들링) 결과 | **그대로** |

같은 CSV · 같은 시드면 같은 결과가 나온다.
**기존에 뽑아둔 용적률·리핸들링 실험 수치를 다시 돌릴 필요 없다.**

---

## 1. 변경 ① — 가중치로 해를 고르는 방식 (핵심)

### 증상

가중치 슬라이더를 0에서 100까지 끝까지 움직여도 용적률이 **딱 3개 값**에서만 바뀌었다.

### 원인

`pick_by_weight()`가 **선형 가중합**을 썼다.

```
score = w · (정규화 용적률) + (1 − w) · (정규화 리핸들링)   → 최대화
```

유한한 점 집합에서 선형 함수를 최대화하면, w를 0~1 사이 어떻게 바꿔도
**볼록 껍질(convex hull)의 꼭짓점**에 해당하는 점만 선택된다.
가중합 스칼라화의 잘 알려진 한계다.

그런데 우리 프론트는 리핸들링이 정수라 **계단(staircase) 모양**이 되고,
계단 점 대부분이 껍질 안쪽으로 들어간다.
→ 프론트에 20~30개 해가 있어도 **어떤 가중치로도 도달 불가능한 해가 대다수**였다.

### 수정 — 증강 가중 체비셰프 스칼라화

```
d(해) = max( w · Δ용적률, (1 − w) · Δ리핸들링 ) + ρ · (Δ용적률 + Δ리핸들링)   → 최소화
        (Δ = 이상점 대비 편차, ρ = 1e-3)
```

이상점까지의 "가중 최대 편차"를 최소화하는 방식이라,
볼록하지 않은 구간에 있는 해도 어떤 가중치에서는 반드시 유일 최소값이 된다.
→ **프론트 위의 모든 파레토 해가 슬라이더로 도달 가능**해진다.

ρ 항은 체비셰프 단독으로는 약하게 지배되는 해가 동점으로 뽑힐 수 있는 문제를 막는 표준 보정항이다.

### 실측 (`modified_BR6_full.csv` instance 1, 20ft — 프론트 12개 / 비지배 5개)

| 방식 | 슬라이더 전 구간에서 도달 가능한 해 |
|---|---|
| 선형 가중합 (기존) | **3개** — (66.79%, 26건) (71.4%, 36건) (71.54%, 40건) |
| 체비셰프 (수정 후) | **5개 = 비지배 해 전부** |

나머지 7개는 두 지표 모두 열세인 피지배 해라 선택되지 않는 게 맞다.

### 양 끝 동작은 불변 — 중요

- `w = 1` → 최대 용적률 (= `pick_three()`의 `max_fill`)
- `w = 0` → 최소 리핸들링 (= `min_reloc`)

동점 처리 규칙(동점이면 용적률 높은 쪽 → 그래도 동점이면 리핸들링 적은 쪽)도
`pick_three()`와 같게 맞춰뒀다. **프리셋 카드 3개의 결과는 이전과 완전히 동일하다.**

---

## 2. 변경 ② — 슬라이더 유효 범위 트리밍 (기능 추가)

이전엔 슬라이더 양 끝에 "움직여도 아무것도 안 바뀌는" 긴 구간이 있었다
(예: w = 0.00 ~ 0.31이 전부 같은 해).

가중치 0~1 전 구간을 1,000스텝으로 스캔해 해가 바뀌는 지점을 모두 찾고,
슬라이더에는 `[첫 변화점 − 여유, 마지막 변화점 + 여유]` 범위만 매핑한다.
여유를 남긴 건 양 끝 극단해가 슬라이더 끝에서 여전히 선택 가능해야 하기 때문 —
데드존을 없애는 게 아니라 **한 칸 폭으로 줄이는** 것이다.

BR6 실측: 유효 범위 `0.145 ~ 1.0` (데드존 14%p 제거). 프론트가 클수록 효과가 커진다.

> ⚠️ **슬라이더 위치 ≠ 가중치**가 됐다. 위치 0%가 가중치 0%가 아니다.
> 그래서 화면에 실제 가중치를 항상 표시한다(아래 ③).

---

## 3. 변경 ③ — 가중치율 표시 (기능 추가)

| 위치 | 표시 |
|---|---|
| 핸들 따라다니는 말풍선 | `용적률 60 : 40 리핸들링` |
| 헤더 상시 표시 | `가중치 용적률 60% · 리핸들링 40%` |
| 눈금 툴팁 | (기존) "여기서부터 바뀝니다" → **"용적률 34% : 리핸들링 66% 지점"** |

---

## 4. 변경 ④ — PDF 명세서 링크 버그 수정

슬라이더 위치 ≠ 가중치가 되면서 가중치가 `0.317` 같은 값이 될 수 있는데,
PDF 링크가 소수 2자리로 반올림(`toFixed(2)` → `0.32`)하고 있었다.
→ **화면과 다른 해의 명세서가 나올 수 있었다.** 3자리로 수정.

---

## 5. 검증

- **서버-프론트 교차 대조**: 같은 프론트 데이터에 대해
  Python `pick_by_weight()` 와 JS `wbPickIndex()` 가
  가중치 1,001개 지점에서 고르는 해를 전수 비교 → **불일치 0건**.
  슬라이더 눈금 위치가 실제 변화 지점과 정확히 일치함을 확인.
- 두 파일 문법 검사 통과 (`ast.parse`, `node --check`).

---

## 6. 예상 질문 — 미리 답 정리

### Q. 슬라이더를 움직이면 왜 값이 연속으로 안 바뀌고 뚝뚝 끊기나?

**① 슬라이더는 계산하지 않는다.**
`/api/reweight`는 이미 계산해 둔 해 목록에서 하나를 **고를 뿐**이다.
NSGA-II를 다시 돌리지 않는다. 목록에 없는 값은 나올 수 없다.

**② 적재 계획은 물리적 배치다.**
용적률 76.43%와 76.44% 사이에 중간이 없다.
실제로 그 용적률을 내는 배치가 존재해야만 그 숫자가 존재한다.

**③ 결정적으로 리핸들링은 정수다.**
파레토 최적의 정의상, 정수 r 하나당 용적률이 가장 높은 해 딱 1개만 비지배다.

> **파레토 해 개수 ≤ 프론트에 등장하는 서로 다른 리핸들링 값의 개수**

BR6에서 리핸들링이 26~43 범위였으니 **이론상 상한이 18개**.
알고리즘 문제가 아니라 문제 자체의 성질이다.

---

## 7. 아직 안 한 것 — 다음 개선 1순위

`clp/nsga2.py` 마지막 부분:

```python
fronts = fast_nondominated_sort(pop)
front0 = [pop[i] for i in fronts[0]]
return front0
```

**마지막 세대 개체군의 rank-0만** 반환한다.
`pop_size=30`이면 최대 30개, 그중 (용적률, 리핸들링) 기준 비지배는 보통 5~10개뿐이다.

그런데 NSGA-II는 30~80세대를 도는 동안 **수백~수천 개**의 배치를 실제로 평가한다.
중간 세대에서 나왔던 좋은 해들이 개체군 교체 과정에서 전부 버려지고 있다.
**슬라이더 눈금이 적은 진짜 병목은 이쪽이다** — 탐색이 못 찾아서가 아니다.

### 제안: 외부 아카이브(external archive)

모든 세대에서 나온 비지배 해를 따로 누적해 두고,
마지막에 그 전체의 비지배 집합을 프론트로 반환한다.

- **추가 계산 비용 거의 0** (이미 평가한 결과를 버리지 않고 모으기만 함)
- 슬라이더 눈금과 파레토 그래프 점이 함께 촘촘해짐
- 다목적 최적화의 표준 기법

---

## 8. 참고 — 같이 정리하면 좋은 것

서버 `pick_three()`가 `duplicate: true` 플래그를 내려주는데
**프론트가 한 번도 사용하지 않는다** (`index.html` 내 등장 0회).
"최대 용적률 카드와 균형 카드 수치가 같다"는 지적은 이 플래그로 바로 처리 가능하다.

### 변경된 함수

| 파일 | 함수 |
|---|---|
| `clp/solutions.py` | `pick_by_weight()` — `cheby_dist()` 도입, `max()` → `min()` |
| `web/index.html` | `wbPickIndex()` · `buildWeightMap()` · `posToWeight()` · `updateWeightLabels()` · `renderWeightTicks()` · `applyWeight()` |

두 파일의 선택 로직은 **완전히 같은 식**이어야 한다.
한쪽만 고치면 눈금 위치가 실제 변화 지점과 어긋난다.
