# LCL 3D 적재 최적화 프로젝트 — 확정 사안 및 근거 정리

> 작성 기준일: 2026-08-03
> 목적: modified BR 데이터 및 알고리즘 설계에 대해 지금까지 확정한 결정과, 그 근거가 된 도메인 지식·출처를 한 곳에 정리

---

## 0. 전체 설계 골격

- **학술 기반**: Elahi et al. (2025), *Multi-Objective Optimization of Container Loading: Balancing Space Utilization, Unloading Obstacles, and Cargo Stability* (Research Square 프리프린트, DOI 10.21203/rs.3.rs-7712709/v1)
- **역할 분담**:
  - 프리프린트 = **알고리즘 설계 청사진** (목적함수 3종, DBLF 디코더, 이배체 염색체, NSGA-II 루프)
  - Kuo, Ho, Zulvia (2023), *Applied Soft Computing* 143:110417 = **출판 근거** (인용·성능 방어용, 프리프린트가 직접 벤치마킹한 논문)
- **데이터 기반**: BR(Bischoff & Ratcliff) 인스턴스 thpack1~7, OR-Library 공개

---

## 1. 데이터셋: BR 인스턴스 (원본 유지)

### 확정 사안
- thpack1~7 **전부 사용** (BR1~BR7, 이질성 3종→20종으로 점증)
- 컨테이너는 파일에 고정: **587 × 233 × 220 cm ≈ 30.1 CBM**
- 원본 데이터(크기·회전플래그·수량)는 **수정하지 않고** 그 위에 컬럼을 추가

### 근거가 된 도메인 지식
- **BR은 실제 선적 기록이 아니라 "현실적 규칙으로 생성한 합성 벤치마크"**. 컨테이너 규격은 실제 20ft에 근접하고, 박스는 종횡비 제약을 둔 난수 생성. 이질성을 BR1→BR7로 단계적으로 설계해 현장 스펙트럼을 커버. → 학술 비교(공정성)에는 최적이나 무게·하역·파손 등 운영 제약은 비어 있어 우리가 합성해야 함.
- **물량 검증 (직접 계산)**: thpack2의 여러 인스턴스에서 박스 총부피가 컨테이너 용적의 **99~100%**로 일정. 박스 개수는 인스턴스마다 81~201개로 다르지만 총부피는 항상 1대분. → BR은 "1대에 살짝 넘치게" 의도 설계되어, 단일 컨테이너 최적화에 이상적(다 못 싣기 때문에 '무엇을 실을지' 선택 문제가 자연 발생).
- **이상적 물량 범위**: 박스 총량이 컨테이너의 100~130%일 때 최적화가 의미 있음. 50%면 다 들어가 천장이 고정, 200%면 '무엇을 버릴지' 문제로 변질. BR은 ≈100%로 이 범위에 정확히 위치.

### thpack 파일 포맷 (컬럼 정의 — 파일엔 헤더 없음)
출처: OR-Library 포맷 설명 페이지 `thpackinfo.html` + 원 논문
```
첫 줄: 인스턴스 개수 P
각 인스턴스:
  줄1: 인스턴스번호  seed값
  줄2: 컨테이너 L  W  H
  줄3: 박스종류수 n
  박스 n줄: type_id  dim1 flag1  dim2 flag2  dim3 flag3  quantity
```
- 3개의 flag(0/1) = 각 축을 수직으로 세울 수 있는지 → 논문 C2(6방향 회전) 하드 제약과 대응

---

## 2. 추가 컬럼: modified BR 변환 규칙

### 확정 사안 (컬럼별)

| 컬럼 | 규칙 | 상태 |
|------|------|------|
| L / W / H, 회전플래그 | 원본 BR 그대로 | 확정 |
| **weight** | **순수 부피 정비례** (논문과 동일, 현실성 유지) | 확정 |
| **destination(하역지)** | **2~3개** (현실적), 우선순위의 근원 | 확정 |
| **priority** | **하역지 방문 순서** (부피와 무관 ← 논문과 핵심 차이) | 확정 |
| **shipper(화주)** | 하역지 내 여러 명, 비용배분용(최적화 밖) | 확정 |
| **max_stack** | **이진 0/1, 파손주의 10%** (1순위). 이후 다단계 확장 | 확정 |

### 각 결정의 근거

**(a) weight = 순수 부피 정비례**
- 프리프린트 규칙 그대로: "원본 BR은 무게가 없어, 부피에 비례하는 합성 무게를 부여, 큰 박스=무겁게." 선행 CLP 연구들과 일관.
- 우리는 문제를 과도하게 어렵게 만들지 않기 위해 밀도 분산 없이 순수 정비례 채택.
- 주의: BR은 cm 단위 → CBM 환산 시 ÷1,000,000. C7(무게한도)는 컨테이너 max_payload 초과 여부만 확인.

**(b) 배송 우선순위 = 하역지 방문 순서 (부피 역비례 아님)**
- 프리프린트는 "우선순위를 박스 부피에 역비례"로 부여 → 작은 박스=먼저 배송. 이는 **현실 반영이 아니라 DBLF가 큰 박스를 안쪽에 넣는 성질과 맞물려 LIFO가 저절로 지켜지게 만든 데이터 세팅상의 편법** (논문도 warm-start 설명에서 "LIFO를 암묵 지원"한다고 명시).
- 우리 현실: 우선순위 = **항구 방문 순서(하역지 A→B→C)**로, 화물 크기와 무관. 큰 화물이 먼저 내릴 수도 있음. → 부피 역비례를 따르지 않음.
- 결과: 우리 ULO는 논문보다 정직하게(대체로 높게) 나올 수 있음. 이는 현실성이 높다는 뜻이며 단점 아님. 단, **논문 Table 3 ULO 수치와 직접 비교 불가**, 경향성만 비교.

**(c) 하역지 2~3개 + 한 화주=한 하역지 가정**
- LCL 혼적의 기본 구조는 "여러 화주 → 같은 목적지 컨테이너 공유". 콘솔리데이터가 목적지 항구별로 화물을 그룹핑하고, 하나의 consolidation은 단일 목적지(또는 인접 항구군) 단위.
  - 근거: gofreight.com/glossary/lcl (콘솔리데이터가 목적지별 그룹핑, HBL/MBL 구조)
  - 근거: suaidglobal.com (consolidation은 단일 목적지 항구, LA·NY면 별도 2건)
- 한 화주가 여러 하역지에 보내는 경우는 개별 화주 차원엔 있으나 **하나의 공유 컨테이너 안에서는** 거의 없음(목적지 다르면 다른 컨테이너로 분리). → "한 화주=한 하역지" 가정은 단일 컨테이너 모델에서 타당.
- multi-drop = 컨테이너가 여러 하역지를 순차 방문하는 것이지 개별 화물이 쪼개지는 게 아님. 인접 항구를 함께 도는 관행이 정확히 이 시나리오.
  - 근거: globalialogisticsnetwork.com (같은 목적지/지역으로 향하는 화물 결합)

**(d) 하역지당 화주 수**
- 업계 표준 통계값은 없음(화물 크기·항로·콘솔리데이터에 따라 상이).
- 물리적 추정: 화주당 물량 1~15 CBM, 최소청구 1~2 CBM → 20ft 컨테이너에 **화주 약 4~8명** (소량이면 더 많음). 하역지 2~3개로 나누면 **하역지당 2~4명**.
  - 교과서적 예시: 의류·전자·수공예 3개 화주가 40ft 로테르담行 공유 (frayto.com)
- 화주 수는 최적화(용적률·ULO·CG)에 **영향 없음** — 하역지만 최적화에 관여, 화주는 비용배분 후처리에만 사용. → "현실적으로 보이게"만 정하면 충분.
- 권장값: 하역지별 화주 수 불균등(예: 4/3/2명), 화주당 물량도 불균등.

**(e) stackability = 이진 0/1, 파손주의 10% (1순위)**
- 프리프린트: 이진값. 0=파손주의(위에 아무것도 못 올림), 1=완전 적재가능(무게한도 없음). 등급별 하중은 future work.
- 완화 논리: 파손주의(0) 박스 위는 죽은 공간 → 파손주의 비율↓ = 용적률↑. 이는 우리가 정량적으로 보일 수 있는 결과.
- **단계적 접근 확정**:
  1. 1단계: 이진 0/1, 파손주의 10% → 기준선 (구현 단순, 논문 비교 깔끔)
  2. 2단계: 파손주의 비율(30/20/10/0%) 변화 → "완화 효과" 정량화
  3. 3단계: 다단계 max_stack(1~4) → "다단계의 추가 이득" 정량화 (계획서의 "쌓기 제한", 논문 future work 구현)
- 이진을 baseline으로 먼저 둬야 다단계 개선폭을 숫자로 제시 가능.

---

## 3. 컨테이너 대수 / 혼적 구조

### 확정 사안
- **1순위: 단일 컨테이너 최적화** (여러 화주 공유 + multi-drop 하역지). 논문과 동일 단위, BR 물량과 정합.
- **확장(나중): 다중 컨테이너 배정** ("최소 컨테이너 할당", 계획서 기재) — 단일 컨테이너가 안정된 뒤.
- 화물 총량은 컨테이너 1대분으로 설정 (BR이 이미 그러함).

### 근거가 된 도메인 지식
- 콘솔리데이터는 "1대 채우고 끝"이 아니라 **주간 정기 스케줄**로 운영. 물량 많으면 같은 목적지로 여러 대, 적으면 1대/격주.
  - 근거: suaidglobal.com (주간 마감·금요일 consolidation), worldzoneglobal.com (weekly departures)
- **컨테이너 배정은 목적지 기준으로 거의 자동 결정** → 진짜 최적화 대상은 "각 컨테이너 내부 적재". 우리 시뮬레이터가 다루는 단위가 바로 이것.
- 여러 화주의 한 컨테이너 공유는 예외가 아니라 **LCL의 정의 자체** → 반드시 기본 전제로 반영.

### 관련 미결 사안
- **못 실은 박스 처리**: BR은 박스가 살짝 넘쳐 다 못 실음. 하역지·화주 부여 후 "화주 단위로 온전히 싣기 강제 vs 부분 적재 허용" 규칙 필요. Phase 1에선 용적률 극대화(임의 잔여)로 두고 이후 정의.

---

## 4. 화주별 비용 배분 (후처리, 최적화 밖)

### 확정 방향
- LCL 표준 **revenue ton (W/M) 기준**으로 혼적 운임을 화주별 배분.

### 근거가 된 도메인 지식
- **revenue ton = W/M 규칙**: 1 RT = 1 CBM 또는 1,000 kg 중 **큰 값**. LCL 운임의 표준 청구 단위.
  - 근거: calculatecbm.com, tracecontainer.com, seko logistics — "whichever is greater"
- **비용 배분 공식**: 화주 비용 = (화주 개별 RT / 전체 RT 합) × 총 운임.
  - 근거: logisticscalc.com (Proportional Allocation), 콘솔리데이터가 실제 사용 공간/중량으로 분배
- 화주 컬럼은 이 배분 계산에만 쓰이고 세 목적함수에는 들어가지 않음.

---

## 5. 알고리즘 설계 (프리프린트 기반)

### 확정 구조 (프리프린트 그대로 구현)
- **HGA = NSGA-II(전역) + DBLF 디코더(그리디 배치)**. 3목적 **동시** 최적화(용적률↑, ULO↓, CG편차↓).
- **이배체 염색체**: 염색체1=박스 순열(순서), 염색체2=회전(6방향 중 1).
- **DBLF 디코더**: 후보점 P를 (x,z,y) 사전식 정렬(깊이→낮이→왼쪽), 실현가능한 첫 자리에 배치, 후보점 갱신·pruning. 원점 FBL(0,0,0), +x=안쪽(문 반대), +y=오른쪽, +z=위.
- **박스 랭킹**(디코딩 직전): 고객우선순위 → stackability → 부피 → 밑면적 → 최대치수.
- **NSGA-II 루프**: binary tournament 선택 → order-based crossover(염색체1)+PMX(염색체2) → 2-OPT 변이(pm1)+회전 리셋(pm2) → 비지배 정렬+crowding → 상위 N 생존.
- **파라미터**: pop 50, gen 200, Cp 0.8, pm1 0.6, pm2 0.3.

### 중요 주의점 (계획서 vs 논문 충돌)
- 수행계획서는 LIFO를 "하드하게 강제"하는 뉘앙스("마지막 도착지 안쪽, 첫 도착지 문 쪽 자동 배치").
- **그러나 논문 결론 = LIFO를 하드 제약으로 강제하면 packability 붕괴 → ULO는 반드시 소프트 목적으로 처리**. (우리 6번 실험 독립 확인과 동일)
- 따라서 하역지·우선순위를 데이터에 넣는 목적은 "LIFO 강제"가 아니라 **"ULO를 계산해 소프트하게 최소화"**. 알고리즘은 우선순위 위반 배치도 허용하되 ULO로 카운트.

---

## 6. 재현 한계 (반드시 인지)

- 프리프린트는 프리프린트(미출판)라 재현 세부가 부족:
  - **명시됨**: 무게=부피비례, 고객수=BR클래스(3~20), 우선순위=부피역비례, stackability=이진, CG중점(293.5,116.5,110.0)/±10%
  - **미명시(빈칸)**: 박스→고객 배정 구체 방법, 파손주의 비율, 무게 비례상수, 고객단위 우선순위 결정법
- → 논문 Table 3·4 수치의 ±1%p 재현은 사실상 불가능. **우리 규칙을 명시적으로 정하고 문서화**하여 "우리 세팅에서 재현 가능"을 목표로 함(경향성 재현).
- 우리는 어차피 하역지 정의(부피 무관)·하역지 수(2~3)·stackability(완화) 등에서 논문과 의도적으로 다르므로, 논문 복제가 아니라 "논문 기반 확장"이 목표.

---

## 부록: 주요 출처

**학술**
- Elahi et al. (2025) 프리프린트, DOI 10.21203/rs.3.rs-7712709/v1
- Kuo, Ho, Zulvia (2023), Applied Soft Computing 143:110417
- Gehring & Bortfeldt (1997) — 용적률 벤치마크 고전
- Bischoff & Ratcliff (1995), OMEGA — BR 데이터 원전

**데이터**
- OR-Library thpack: people.brunel.ac.uk/~mastjjb/jeb/orlib/thpackinfo.html

**LCL 도메인 (현장 근거)**
- gofreight.com/glossary/lcl — 콘솔리데이터 목적지 그룹핑, HBL/MBL
- suaidglobal.com/insights/lcl-consolidation-guide — 단일목적지 consolidation, 주간 스케줄
- globalialogisticsnetwork.com — 같은 목적지/지역 화물 결합
- frayto.com/blogs/consolidated-shipping-benefits — 다화주 공유 예시
- calculatecbm.com, tracecontainer.com, sekologistics.com — revenue ton(W/M) 규칙
- logisticscalc.com — RT 비례 비용배분 공식
