# NSGA-II + DBLF 구현 확정사항

> LCL 다중 하역지 컨테이너 적재 최적화 — 알고리즘 설계 결정 정리
> 기준 논문: Elahi et al. (2025), *Multi-Objective Optimization of Container Loading*, Research Square

---

## 1. 문제 정의 (확정본)

- **목적함수 3개** (tri-objective)
  - `Z1` 용적률 최대화 = 적재 부피 합 / 컨테이너 부피
  - `Z2` ULO(하역 장애물) 최소화 = 먼저 내릴 화물을 나중 화물이 안쪽(+x)/위(+z)에서 막는 쌍의 개수
  - `Z3` CG 편차 최소화 = 3축 절대편차 평균 (컨테이너 중점 기준)
- **컨테이너**: 587 × 233 × 220 cm (40ft GP)
- **좌표계**: 원점 front-bottom-left, +x=안쪽(문 반대), +y=오른쪽, +z=위

## 2. 논문 대비 변경점

| 항목 | 논문 | 우리 |
|---|---|---|
| π(하역순서) | 고객 | `priority` (=destination, 1:1) |
| 화주(shipper) | 없음 | 목적함수 밖, 운임 배분용으로 격리 |
| CG 무게 | 부피 프록시 | `weight_kg` 실측(밀도 300kg/m³) |
| 안정성 θ | 0.75 | **0.70** |
| stackable | 이진 | 이진, 해석 A (=0이면 그 위에 적재 불가) |
| 회전 | 6방향 | 6방향 (vert_axis_allowed 무시) |
| CG 하드캡 | 옵션 | 미적용 (soft만) |

## 3. 알고리즘 설계 결정

### B4 — 미적재 처리 (전량 적재 원칙)
- **모든 박스 적재를 원칙으로 함.**
- 한 컨테이너에 다 못 넣으면:
  - 못 실은 화물의 **개수·화주**를 리포트
  - 다음 컨테이너로 넘김
  - **용적률은 첫 번째 컨테이너 기준으로 계산**
- CG(Z3) 꼼수 해(박스 몇 개만 놓고 CG 맞추기)는 전량 적재 원칙으로 자연 방지

### B1 — priority 블록 구조
- 염색체를 **priority 오름차순 블록**으로 유지 (priority1 앞 → priority2 → priority3 뒤)
- crossover/mutation은 **각 블록 내부에서만** 수행, 블록 경계 불변
- ULO의 strict inequality(π(i)<π(k)) 구조와 자동 정합

### B2 — 회전 염색체 crossover
- 논문의 PMX(순열 전용, 회전값엔 부적합)를 **uniform crossover로 대체**

### B3 — 회전 리셋 단위
- **박스 단위 리셋** (타입 동일 회전 규칙은 초기화에만 적용, 진화 중엔 완화)

### B5 — 지지면적 계산
- 박스 밑면과 바로 아래 박스들 윗면의 **사각형 겹침 면적 합산** → 밑면적의 70% 이상이면 안정

### B6 — balance tie-break
- (x,z,y) 사전식 정렬이 1차 기준
- **완전 동률일 때만** moment difference로 tie-break
- CG는 Z3가 전역 최적화하므로 로컬 보정은 최소

## 4. 파라미터 (논문 Table 1)

| 파라미터 | 값 |
|---|---|
| Population size (N) | 50 |
| Generations (G) | 200 |
| Crossover rate (Cp) | 0.8 |
| Mutation pm1 (2-OPT) | 0.6 |
| Mutation pm2 (회전 리셋) | 0.3 |

## 5. 비교 기준 (C1)

- 논문 재현·비교는 **목적 아님**. 완성 자체가 목표.
- 나중에 우리 그리디(~72%) / LIFO(~53%)와 비교.
- 논문은 방법론 출처로만 인용.

## 6. 향후 기능 (핵심 완성 후)

- **컨테이너 자동 추천**: 화물에 맞는 C20/C40/C40HC 선택 (실무 포워더 로직)
- **미적재 화물 리포트**: roll-over 의사결정 지원
- 시뮬레이터 통합: FastAPI 백엔드 + 비동기 + 진행률 (CSV in → 좌표 JSON out)
