# 시뮬레이터 실행 방법

## 1. 최초 1회 — 패키지 설치

```powershell
pip install fastapi uvicorn python-multipart
```

## 2. 서버 실행

```powershell
cd C:\Users\wjduc\Desktop\NSGA
python server.py
```

터미널에 아래가 뜨면 준비 완료입니다.

```
  적재 최적화 서버 시작
  → http://localhost:8000 을 브라우저에서 열어주세요
```

## 3. 브라우저에서 열기

주소창에 `http://localhost:8000` 입력.

## 4. 사용 순서

1. 좌측에 CSV 파일을 끌어놓거나 클릭해서 선택
2. 컨테이너 규격 확인 (추천이 미리 선택됨)
3. 계산 방식 선택
   - **정밀 계획**: NSGA-II, 세 가지 대안 제시 (1~2분)
   - **빠른 대응**: 휴리스틱, 단일 해 (수 초)
4. `적재 계획 세우기` 클릭
5. 계산이 끝나면 상단 탭에서 대안 전환, 하단에서 애니메이션 재생

## 화면 조작

| 조작 | 동작 |
|---|---|
| 드래그 | 시점 회전 |
| 휠 | 확대·축소 |
| ▶ | 적재 순서 재생 |
| 슬라이더 | 임의 시점 이동 |
| ⇤ | 하역 시뮬레이션 (재작업 화물이 붉게 표시) |
| 좌측 세로 띠 클릭 | 입력 패널 다시 펼치기 |

## 종료

터미널에서 `Ctrl + C`
