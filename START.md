# 시뮬레이터 실행 방법

## 설치 없이 결과만 볼 때

<https://mir-yong.github.io/cargo-fit/>

미리 계산해 둔 결과로 3D 뷰어 · 적재안 비교 · 적재 순서 재생이 그대로 동작합니다.
새 CSV로 직접 계산하려면 아래처럼 내 컴퓨터에서 서버를 띄워야 합니다.

---

## 1. 최초 1회 — 설치

**Python 3.10 이상이 필요합니다.** (3.9 이하에서는 서버가 시작되지 않습니다)

```bash
python3 --version        # 3.10+ 인지 확인
pip install -r requirements.txt
```

## 2. 서버 실행

프로젝트 폴더에서:

```bash
python3 server.py
```

아래가 뜨면 준비 완료입니다.

```
  적재 최적화 서버 시작
  → http://localhost:8000 을 브라우저에서 열어주세요
```

종료는 터미널에서 `Ctrl + C`.

## 3. 사용 순서

화면은 두 단계입니다 — **01 화물 등록** → **02 최적화 결과**.

**01 화물 등록** (좌: 화물 명세 / 우: 적재 조건)

1. 좌측 카드의 **업로드 영역**에 CSV를 끌어놓거나 클릭해서 선택
   - 형식을 모르면 바로 아래 `CSV 템플릿 다운로드`를 눌러 받으면 됩니다
   - 업로드하면 같은 자리에 화물 수 · 총 부피 · 총 중량과 화주별 내역이 나옵니다
2. 우측 카드에서 **컨테이너 규격**과 **적재 방식**을 확인 (추천이 미리 선택돼 있습니다)
3. 필요하면 **고급 설정**을 펼쳐 조정 — 지지율 · 무게중심 허용오차 · 최대 적재 단수 · 구역 제약
4. 우측 카드 맨 아래 **계산하기** 클릭 (정밀 탐색, 약 1분)

**02 최적화 결과**

5. 계산이 끝나면 결과 화면으로 자동 전환됩니다
   - 상단의 **적재안 3종 카드**(최대 용적률 / 균형 / 최소 리핸들링)를 클릭해 전환
   - 좌측에서 3D 확인, 그 아래 **직접 조정** 슬라이더로 세 대안 사이 임의 지점 선택
   - 우측에서 적재 품질 · 지표 4종 · **검토 필요** 항목 확인, 그 아래 파레토 분포

## 화면 조작

| 조작 | 동작 |
|---|---|
| 드래그 | 시점 회전 |
| 휠 | 확대 · 축소 |
| 화물 클릭 | 그 화물의 하역지 · 치수 · 중량 · 적재 단수 · 순서 표시 |
| `3D` `TOP` `SIDE` `DOOR` | 시점 전환 (3D 카드 헤더 우측) |
| 3D 화면 우측 하단 아이콘 | 컨테이너 맞춤 · 기본 시점 · 전체화면 |
| ▶ | 적재 순서 재생 / 일시정지 (3D 화면 바로 아래 재생 바) |
| `1x` `2x` `4x` | 재생 속도 |
| 재생 바 슬라이더 | 임의 시점으로 이동 |
| ⇤ | 하역 시뮬레이션 (재배치가 필요한 화물이 붉게 표시) |
| 헤더의 `1` `2` | 단계 이동 (계산 결과가 없으면 `2`는 잠겨 있습니다) |
| `← 화물 등록으로` | 결과 화면에서 입력 화면으로 되돌아가기 |
| 직접 조정 슬라이더 | 용적률 ↔ 리핸들링 균형점 직접 선택 |
| 파레토 그래프의 점 | 클릭하면 그 적재안으로 전환 |

## CSV 형식

필수 컬럼:

```
box_id, length_cm, width_cm, height_cm, weight_kg, destination, priority, shipper
```

선택 컬럼:

| 컬럼 | 의미 | 기본값 |
|---|---|---|
| `type_id` | 규격 종류 번호 | 0 |
| `stackable` | 0이면 이 화물 위에 적재 금지 | 1 |
| `fragile` | 1이면 파손주의 — 이 화물 위에 적재 금지 | 0 |

> **주의**: 같은 `destination`은 모두 같은 `priority`를 가져야 합니다.
> 목적지별 LIFO(후입선출) 하역 순서를 검증하기 위한 규칙이며, 어긋나면 업로드가 거부됩니다.

## 잘 안 될 때

| 증상 | 원인 · 해결 |
|---|---|
| 서버가 시작되지 않음 | Python 3.10 이상인지 확인 |
| `ModuleNotFoundError` | `pip install -r requirements.txt` 실행 |
| 3D 화면이 안 보임 | 인터넷 연결 확인 (Three.js와 폰트를 CDN에서 받습니다) |
| 업로드 시 400 에러 | 메시지에 어느 하역지의 priority가 어긋났는지 나옵니다 |
| 명세서 PDF만 실패 | wkhtmltopdf 미설치. 맥은 `brew install wkhtmltopdf` |
| 계산이 유난히 느림 | 이전 계산이 백그라운드에 남아 있을 수 있습니다. 서버를 재시작하세요 |
